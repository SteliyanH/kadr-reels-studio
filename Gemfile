source "https://rubygems.org"

# v0.6 Tier 8 — fastlane pins for release engineering. Bump deliberately,
# not on every `bundle update` — fastlane's plugin ABI shifts have broken
# CI lanes before.
gem "fastlane", "~> 2.225"
